export interface ISalesQuotationDetailsData {
    id: number, item_code: string,  qty: number,  available: number, selling_price: number, total: number, sales_quotation_id: number
}
export interface  ISalesQuotationDetailsParams {
    id?: number,item_code: string,  qty: number,  available: number, selling_price: number, total: number, sales_quotation_id: number
}
