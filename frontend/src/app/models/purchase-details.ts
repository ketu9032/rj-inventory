export interface IPurchaseDetailsData {
    id: number, item_code: string,  qty: number,  available: number, selling_price: number, total: number, purchase_id: number
}
export interface  IPurchaseDetailsParams {
    id?: number,item_code: string,  qty: number,  available: number, selling_price: number, total: number, purchase_id: number
}
